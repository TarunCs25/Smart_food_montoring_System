
  # Smart Food Monitoring System

  ## Running the code

  Take the complete projtct to Local System

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
